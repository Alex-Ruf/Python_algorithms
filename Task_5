# https://drive.google.com/file/d/1gRvpEIPaeyyR3nIIeM3MlD0tkRV8PcH2/view?usp=sharing

MIN = 32
MAX = 127
COUNT = 10

def gen(i):
    l = list()
    for a in range(i, i + COUNT):
        if a <= MAX:
            l.append(str(a)+"-"+chr(a))
        else:
            break;
    return tuple(l)


for i in range(MIN, MAX, COUNT):
    c = gen(i)
    print(" ".join(c))
